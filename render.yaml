services:
  - type: web
    name: investpro
    env: python
    rootDir: api
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PORT
        value: 10000
      - key: INGEST_TOKEN
        value: RTD_123456!
    autoDeploy: true
    plan: free
